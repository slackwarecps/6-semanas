Title: You need to integrate Claude with dozens of GitHub endpoints.
---

You need to integrate Claude with dozens of GitHub endpoints. Why use an MCP server instead of defining each tool manually?
[Resposta correta: O ganho é não precisar autorar e manter schema e função de cada capability. A inverte o papel do MCP. C inventa uma conversão de protocolo inexistente. D é falso, a autenticação continua necessária.]

---
[ ] A - Because the MCP server runs Claude locally, dispensing with the API.
[ ] B - Because the MCP server already ships the tools ready, avoiding writing and maintaining schema plus function for each capability.
[ ] C - Because MCP automatically converts REST APIs into GraphQL.
[ ] D - Because MCP eliminates the need for authentication on GitHub.

---
Tags: Domain_2::Tool_Design_MCP_Integration
