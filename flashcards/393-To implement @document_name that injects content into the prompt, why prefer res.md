Title: To implement @document_name that injects content into the prompt, why prefer resources over tools?
---

To implement @document_name that injects content into the prompt, why prefer resources over tools?
[Resposta correta: O resource injeta contexto direto no prompt, poupando chamadas de tool extras. A é falso sobre tools. C confunde controle (resources são controlados pela aplicação). D inventa uma diferença de autenticação.]

---
[ ] A - Because tools cannot return text to Claude.
[ ] B - Because the resource injects the content directly into the prompt as initial context, avoiding extra tool calls and generating immediate responses.
[ ] C - Because resources are executed by the model automatically.
[ ] D - Because tools require authentication and resources do not.

---
Tags: Domain_2::Tool_Design_MCP_Integration
